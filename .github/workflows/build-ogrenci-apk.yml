name: Ogrenci APK Build

on:
  push:
    paths:
      - 'android-ogrenci/**'
      - '.github/workflows/build-ogrenci-apk.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: android-ogrenci
    steps:
      - uses: actions/checkout@v4
      - name: JDK 17 kur
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Android SDK kur
        uses: android-actions/setup-android@v3

      - name: Gradle ile derle (imzasiz APK)
        run: |
          chmod +x gradlew
          ./gradlew assembleRelease

      - name: Keystore'u geri yukle
        run: echo "${{ secrets.OGRENCI_KEYSTORE_BASE64 }}" | base64 -d > android.keystore

      - name: Zipalign + Imzala
        run: |
          BUILD_TOOLS=$(ls $ANDROID_HOME/build-tools | sort -V | tail -1)
          $ANDROID_HOME/build-tools/$BUILD_TOOLS/zipalign -v -p 4 \
            app/build/outputs/apk/release/app-release-unsigned.apk karemux-ogrenci-aligned.apk
          $ANDROID_HOME/build-tools/$BUILD_TOOLS/apksigner sign \
            --ks android.keystore --ks-key-alias android \
            --ks-pass "pass:${{ secrets.OGRENCI_KEYSTORE_SIFRE }}" \
            --out karemux-ogrenci-imzali.apk karemux-ogrenci-aligned.apk
          $ANDROID_HOME/build-tools/$BUILD_TOOLS/apksigner verify karemux-ogrenci-imzali.apk

      - name: APK'yi yukle (indirilebilir cikti)
        uses: actions/upload-artifact@v4
        with:
          name: karemux-ogrenci-apk
          path: android-ogrenci/karemux-ogrenci-imzali.apk

      - name: Herkese acik GitHub Release'e yayinla
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ogrenci-apk-latest
          name: "Karemux Ogrenci APK (guncel)"
          files: android-ogrenci/karemux-ogrenci-imzali.apk
          make_latest: false
          body: "Otomatik yayinlanmis, imzali Karemux Ogrenci APK'si. Her yeni derlemede bu ayni link uzerine guncellenir."
