name: Veli APK Build

on:
  push:
    paths:
      - 'android-veli/**'
      - '.github/workflows/build-veli-apk.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: android-veli
    steps:
      - uses: actions/checkout@v4
      - name: JDK 17 kur
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Android SDK kur
        uses: android-actions/setup-android@v3

      - name: Gradle ile derle (imzasiz APK)
        run: |
          chmod +x gradlew
          ./gradlew assembleRelease

      - name: Keystore'u geri yukle
        run: echo "${{ secrets.VELI_KEYSTORE_BASE64 }}" | base64 -d > android.keystore

      - name: Zipalign + Imzala
        run: |
          BUILD_TOOLS=$(ls $ANDROID_HOME/build-tools | sort -V | tail -1)
          $ANDROID_HOME/build-tools/$BUILD_TOOLS/zipalign -v -p 4 \
            app/build/outputs/apk/release/app-release-unsigned.apk karemux-veli-aligned.apk
          $ANDROID_HOME/build-tools/$BUILD_TOOLS/apksigner sign \
            --ks android.keystore --ks-key-alias android \
            --ks-pass "pass:${{ secrets.VELI_KEYSTORE_SIFRE }}" \
            --out karemux-veli-imzali.apk karemux-veli-aligned.apk
          $ANDROID_HOME/build-tools/$BUILD_TOOLS/apksigner verify karemux-veli-imzali.apk

      - name: APK'yi yukle (indirilebilir cikti)
        uses: actions/upload-artifact@v4
        with:
          name: karemux-veli-apk
          path: android-veli/karemux-veli-imzali.apk

      - name: Herkese acik GitHub Release'e yayinla
        uses: softprops/action-gh-release@v2
        with:
          tag_name: veli-apk-latest
          name: "Karemux Veli APK (guncel)"
          files: android-veli/karemux-veli-imzali.apk
          make_latest: false
          body: "Otomatik yayinlanmis, imzali Karemux Veli APK'si. Her yeni derlemede bu ayni link uzerine guncellenir."
